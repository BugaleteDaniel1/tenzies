export default function Title() {
  return (
    <header className="game">
      <header className="game__header">
        <h1 className="game__header-title">TENZIES</h1>
        <p className="game__header-p">
          Roll until all dice are the same click each dice to lock it's current
          value between rolls!
        </p>
      </header>
    </header>
  );
}
